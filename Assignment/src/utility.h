#include <vector>
#include <iostream>
#include <fstream>
#include <string>
#include <vector>
#include <cstring>
#include <sstream>

void loadMap(char * fileName);
vector<char> element;
int width, height;
vector<int> records;
int keyLeft, lifeLeft, point, record ;
int aX, aY;
int lX, lY;
